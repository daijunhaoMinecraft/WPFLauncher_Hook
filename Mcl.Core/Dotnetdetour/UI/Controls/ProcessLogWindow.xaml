<Window x:Class="Mcl.Core.Dotnetdetour.UI.Controls.ProcessLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="进程运行日志" Height="560" Width="900" MinWidth="600" MinHeight="360"
        Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextBrush}"
        WindowStartupLocation="CenterScreen" FontFamily="{DynamicResource UiFont}" UseLayoutRounding="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleTextBlock" Text="运行日志" Style="{StaticResource PageTitle}"
                   TextTrimming="CharacterEllipsis" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,16">
            <Button Content="导出最近日志" Click="ExportLog_Click" Style="{StaticResource PrimaryButton}" />
            <Button x:Name="KillProcessButton" Content="结束进程" Margin="8,0,0,0" Click="KillProcess_Click"
                    Style="{StaticResource DangerButton}" />
        </StackPanel>
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="8">
            <ScrollViewer x:Name="LogScroller" ScrollChanged="LogScroller_ScrollChanged"
                          VerticalScrollBarVisibility="Auto">
                <TextBox x:Name="LogTextBox" Background="Transparent" BorderThickness="0"
                         FontFamily="Cascadia Mono, Consolas" FontSize="13" IsReadOnly="True"
                         TextWrapping="Wrap" AcceptsReturn="True" AutomationProperties.Name="游戏输出日志" />
            </ScrollViewer>
        </Border>
        <TextBlock Grid.Row="3" Margin="0,12,0,0" Style="{StaticResource SecondaryText}"
                   Text="显示最近 50,000 字符，导出保留最近 2 Mi 字符。向上滚动暂停跟随，滚到底部恢复。" />
    </Grid>
</Window>