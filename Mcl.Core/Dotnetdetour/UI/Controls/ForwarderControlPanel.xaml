<Window x:Class="Mcl.Core.Dotnetdetour.UI.Controls.ForwarderControlPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="转发控制台" Height="320" Width="420"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}" FontFamily="{DynamicResource UiFont}"
        Closing="Window_Closing"
        Foreground="{DynamicResource TextBrush}"
        UseLayoutRounding="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CheckBox x:Name="TopMostCheck" 
                  Content="置顶"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Top"
                  Margin="0,4,0,0"
                  Checked="OnTopMostChanged"
                  Unchecked="OnTopMostChanged"/>
        <TextBlock Grid.Row="1" 
                   Text="WebRTC 转发状态"
                    FontWeight="SemiBold" FontSize="20"
                   Margin="0,12,0,8"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,4">
            <TextBlock Text="当前模式:" Width="70"/>
            <TextBlock x:Name="ModeLabel" Foreground="{DynamicResource AccentBrush}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,4">
            <TextBlock Text="转发端口:" Width="70"/>
            <TextBlock x:Name="PortLabel"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,4">
            <TextBlock Text="运行状态:" Width="70"/>
            <TextBlock x:Name="StatusLabel" Text="运行中" Foreground="{DynamicResource SuccessBrush}"/>
        </StackPanel>
        <Button x:Name="StopButton"
                Content="停止转发"
                HorizontalAlignment="Right"
                Style="{StaticResource DangerButton}"
                VerticalAlignment="Bottom"
                Width="120"
                Click="StopButton_Click"
                Grid.Row="5"
                Margin="0,0,0,12"/>
    </Grid>
</Window>