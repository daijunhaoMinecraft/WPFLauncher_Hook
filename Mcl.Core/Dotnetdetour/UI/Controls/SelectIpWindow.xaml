<Window x:Class="Mcl.Core.Dotnetdetour.UI.Controls.SelectIpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="分配虚拟 IP" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{DynamicResource WindowBackgroundBrush}" FontFamily="{DynamicResource UiFont}"
        Foreground="{DynamicResource TextBrush}"
        UseLayoutRounding="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="虚拟网卡 IP 配置" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
            <CheckBox x:Name="TopMostCheck" Content="置顶窗口" Grid.Column="1" VerticalAlignment="Center" Checked="OnTopMostChanged" Unchecked="OnTopMostChanged"/>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <TextBlock Text="请输入主机位 (0-255):" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                
                <Border x:Name="InputContainer" Background="{DynamicResource ControlBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="4" Height="38">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="10.0.0." Grid.Column="0" FontFamily="Consolas" FontSize="16" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="10,0,0,0"/>
                        <TextBox x:Name="LastOctetTextBox" Grid.Column="1" BorderThickness="0" Background="Transparent" FontFamily="Consolas" FontSize="16" FontWeight="Bold" MaxLength="3" VerticalContentAlignment="Center" PreviewTextInput="NumberValidation"/>
                    </Grid>
                </Border>
                
                <TextBlock x:Name="StatusLabel" Foreground="{DynamicResource DangerBrush}" Margin="0,8,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="80" Margin="0,0,12,0" Click="CancelButton_Click"/>
            <Button Content="确认并保存" Width="100" Style="{StaticResource PrimaryButton}" Click="ApplyButton_Click"/>
        </StackPanel>
    </Grid>
</Window>