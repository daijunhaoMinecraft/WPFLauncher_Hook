<Window x:Class="Mcl.Core.Dotnetdetour.UI.Controls.ServerSelectPortWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TCP 端口转发配置" Height="360" Width="360"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="{DynamicResource WindowBackgroundBrush}" FontFamily="{DynamicResource UiFont}"
        Foreground="{DynamicResource TextBrush}"
        UseLayoutRounding="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="TCP 转发配置" FontSize="20" FontWeight="Bold" Foreground="{DynamicResource TextBrush}"/>
            <CheckBox x:Name="TopMostCheck" Content="置顶窗口" Grid.Column="1" VerticalAlignment="Center" Checked="OnTopMostChanged" Unchecked="OnTopMostChanged"/>
        </Grid>

        <Border Grid.Row="1" Background="White" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="16">
            <StackPanel>
                <TextBlock Text="目标 IP:" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox x:Name="IpTextBox" Text="127.0.0.1" Height="36" Margin="0,0,0,16"/>
                <TextBlock Text="端口号:" Foreground="{DynamicResource SecondaryTextBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox x:Name="PortTextBox" Text="25565" Height="36"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Width="80" Margin="0,0,12,0" Click="CancelButton_Click"/>
            <Button Content="确定" Width="80" Style="{StaticResource PrimaryButton}" Click="ApplyButton_Click"/>
        </StackPanel>
    </Grid>
</Window>