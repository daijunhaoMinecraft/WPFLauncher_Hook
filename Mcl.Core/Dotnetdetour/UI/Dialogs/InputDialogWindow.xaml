<Window x:Class="Mcl.Core.Dotnetdetour.UI.Dialogs.InputDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="请输入" Height="220" Width="400"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="{DynamicResource WindowBackgroundBrush}"
        Loaded="Window_Loaded" KeyDown="Window_KeyDown"
        Foreground="{DynamicResource TextBrush}"
        FontFamily="{DynamicResource UiFont}"
        UseLayoutRounding="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.15" Direction="270"/></Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="TxtTitle" Grid.Row="0" Text="请输入存档名称" FontWeight="Bold" FontSize="16" Foreground="{DynamicResource TextBrush}" Margin="20,20,20,10"/>
            <Border Grid.Row="1" Margin="20,10,20,10" Background="{DynamicResource SubtleSurfaceBrush}" CornerRadius="6" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
                <TextBox x:Name="InputTextBox" Background="Transparent" BorderThickness="0" FontSize="14" Padding="10" 
                         VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}" />
            </Border>
            <Border Grid.Row="2" Background="{DynamicResource SubtleSurfaceBrush}" CornerRadius="0,0,12,12">
                <Grid>
                    <Border VerticalAlignment="Top" Height="1" Background="{DynamicResource BorderBrush}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                        <Button Content="取消" Width="80" Height="32" Margin="0,0,10,0" Background="{DynamicResource ControlBrush}" Foreground="{DynamicResource TextBrush}" Style="{StaticResource RoundedBtn}" Click="BtnCancel_Click"/>
                        <Button Content="确定" Width="80" Height="32" Background="{DynamicResource AccentBrush}" Foreground="White" FontWeight="SemiBold" Style="{StaticResource RoundedBtn}" Click="BtnConfirm_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>