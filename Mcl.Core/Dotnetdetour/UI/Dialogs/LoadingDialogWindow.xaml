<Window x:Class="Mcl.Core.Dotnetdetour.UI.Dialogs.LoadingDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="请稍候" Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="{DynamicResource WindowBackgroundBrush}"
        Topmost="True"
        Foreground="{DynamicResource TextBrush}"
        FontFamily="{DynamicResource UiFont}"
        UseLayoutRounding="True">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Mcl.Core;component/Dotnetdetour/UI/Themes/Fluent.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.15" Direction="270"/></Border.Effect>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            
            <TextBlock Text="正在备份至云端..." FontWeight="SemiBold" FontSize="16" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            <ProgressBar IsIndeterminate="True" Width="250" Height="6" BorderThickness="0" Background="{DynamicResource BorderBrush}" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,25">
                <ProgressBar.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </ProgressBar.Resources>
            </ProgressBar>
            <Button Content="取消等待 (后台运行)" Width="130" Height="30" 
                    Background="{DynamicResource ControlBrush}" Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"
                    Style="{StaticResource CancelBtn}" Click="BtnCancel_Click"/>
        </StackPanel>
    </Border>
</Window>