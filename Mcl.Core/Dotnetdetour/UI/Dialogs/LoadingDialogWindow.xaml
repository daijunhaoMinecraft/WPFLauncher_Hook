<Window x:Class="Mcl.Core.Dotnetdetour.UI.Dialogs.LoadingDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="请稍候" Height="180" Width="350"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True">

    <Window.Resources>
        <Style x:Key="CancelBtn" TargetType="Button">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Opacity" Value="0.8"/></Trigger>
                <Trigger Property="IsPressed" Value="True"><Setter Property="Opacity" Value="0.6"/></Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Background="#FFFFFF" CornerRadius="12" BorderBrush="#E5E5E5" BorderThickness="1">
        <Border.Effect><DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.15" Direction="270"/></Border.Effect>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20">
            
            <TextBlock Text="正在备份至云端..." FontWeight="SemiBold" FontSize="16" Foreground="#333333" HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <!-- 跑马灯进度条 -->
            <ProgressBar IsIndeterminate="True" Width="250" Height="6" BorderThickness="0" Background="#E5E5E5" Foreground="#78A65A" Margin="0,0,0,25">
                <ProgressBar.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="3"/>
                    </Style>
                </ProgressBar.Resources>
            </ProgressBar>

            <!-- 取消等待按钮 (防止服务端没响应导致软死机) -->
            <Button Content="取消等待 (后台运行)" Width="130" Height="30" 
                    Background="#F1F3F4" Foreground="#666666" FontSize="12"
                    Style="{StaticResource CancelBtn}" Click="BtnCancel_Click"/>
        </StackPanel>
    </Border>
</Window>